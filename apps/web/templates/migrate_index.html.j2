<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AWS deprecation deadlines — migration guides | EOLkits</title>
    <meta name="description" content="Tracked AWS platform deprecation milestones, ordered by date: Lambda runtimes and Amazon Linux 2. Each links a provider source and bounded migration checks.">
    <meta name="robots" content="index, follow">
    <link rel="canonical" href="{{ site_url }}/migrate/">
    <link rel="stylesheet" href="/style.css">
    <link rel="alternate" type="text/calendar" title="AWS Deprecation Deadlines (EOLkits)" href="/deprecations.ics">
    <meta property="og:title" content="AWS deprecation deadlines — migration guides | EOLkits">
    <meta property="og:description" content="Provider-linked AWS deprecation milestones with bounded free migration tools.">
    <meta property="og:url" content="{{ site_url }}/migrate/">
    <meta property="og:type" content="website">

    <script type="application/ld+json">
    {
        "@context": "https://schema.org",
        "@type": "ItemList",
        "name": "Tracked AWS deprecation deadlines",
        "itemListElement": [
            {% for dep in deprecations %}
            {
                "@type": "ListItem",
                "position": {{ loop.index }},
                "url": "{{ site_url }}/migrate/{{ dep.slug }}/",
                "name": {{ (dep.name ~ " (deadline " ~ dep.date ~ ")")|tojson }}
            }{{ "," if not loop.last }}
            {% endfor %}
        ]
    }
    </script>
</head>
<body class="migrate-page">

<header class="nav">
  <div class="container nav-inner">
    <a href="/" class="brand"><span class="brand-mark">▸</span> EOLkits</a>
    <nav>
      <a href="/migrate/">Deadlines</a>
      <a href="/#pricing">Pricing</a>
      <a href="/deprecations.ics">Calendar</a>
      <a href="https://github.com/ntoledo319/EOLkits" class="btn-ghost">GitHub</a>
    </nav>
  </div>
</header>

<main class="container article">
    <nav class="breadcrumb" aria-label="Breadcrumb">
        <a href="/">Home</a> / <span>AWS deprecations</span>
    </nav>

    <h1>AWS deprecation deadlines</h1>
    <p class="sub">AWS platform deprecations EOLkits tracks, ordered by published deadline. Each page carries sourced dates and bounded migration notes; future schedules can change. <a href="/deprecations.ics">Subscribe to the calendar (.ics)</a>.</p>
    <p class="sub">Prefer a 10-second check instead of browsing the list? Paste your config into the <a href="/eol-checker/">free AWS EOL checker</a> — nothing uploaded. Hitting a specific error message? See the <a href="/fix/">AWS migration error fixes</a> hub.</p>

    <div class="deadline-list">
        {% for dep in deprecations %}
        <a class="deadline-row {{ dep.tier }}" href="/migrate/{{ dep.slug }}/">
            <div class="dl-date">
                <strong>{{ dep.date }}</strong>
                <span class="dl-days">{% if dep.days_until < 0 %}passed{% else %}{{ dep.days_until }} days{% endif %}</span>
            </div>
            <div class="dl-body">
                <div class="dl-name">{{ dep.name }} <span class="severity-badge severity-{{ dep.severity }}">{{ dep.severity }}</span></div>
                <div class="dl-meta">{{ dep.service }}{% if dep.kit %} · <code>{{ dep.kit }}</code>{% endif %}</div>
            </div>
            <div class="dl-arrow">→</div>
        </a>
        {% endfor %}
    </div>

    <p class="cite-note" style="margin-top:2rem">Each date links its provider source. Migration checks are bounded static guidance to validate against the workload and current upstream documentation.</p>
</main>

<footer class="footer">
  <div class="container">
    <div class="footer-grid">
      <div>
        <div class="brand"><span class="brand-mark">▸</span> EOLkits</div>
        <p class="muted">Tracked AWS deprecations deserve tested migration paths.</p>
      </div>
      <div>
        <h5>Deadlines</h5>
        <a href="/migrate/">Tracked AWS deprecations</a><br>
        <a href="/deprecations.ics">Deadline calendar (.ics)</a>
      </div>
      <div>
        <h5>Resources</h5>
        <a href="https://github.com/ntoledo319/EOLkits">GitHub</a><br>
        <a href="/#pricing">Pricing</a>
      </div>
    </div>
    <div class="muted small">© 2026 EOLkits · MIT-licensed kits · facts cited from primary sources</div>
  </div>
</footer>

</body>
</html>
