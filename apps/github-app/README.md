# Archived GitHub App experiment

The former Migration Bot concept is closed. It is not installable, does not
accept payments, does not create pull requests, and is not part of Audit v2.

Its manifest, permissions request, webhook implementation, migration engine,
and credential setup scripts were removed. Git history is the only archive; do
not recreate or publish the App without a new product, security, platform-policy,
and paid-fulfillment review.
