name: Deploy GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - 'docs/**'
      - 'apps/web/**'
      - 'pricing.yml'
      - 'rules/**'
      - '.github/workflows/deploy-pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      TMPDIR: ${{ github.workspace }}/tmp/runtime-tmp
      PIP_CACHE_DIR: ${{ github.workspace }}/tmp/pip-cache
      EOLKITS_BASE_PATH: /EOLkits
      EOLKITS_SITE_URL: https://ntoledo319.github.io/EOLkits
      EOLKITS_API_URL: https://eolkits.com
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
      - name: Build the committed static output
        run: |
          mkdir -p "$TMPDIR" "$PIP_CACHE_DIR"
          python -m venv "$GITHUB_WORKSPACE/tmp/web-venv"
          "$GITHUB_WORKSPACE/tmp/web-venv/bin/pip" install -r apps/web/requirements-dev.txt
          "$GITHUB_WORKSPACE/tmp/web-venv/bin/python" apps/web/build.py
          git diff --exit-code -- docs
      - uses: actions/configure-pages@v6
      - uses: actions/upload-pages-artifact@v5
        with:
          path: docs

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v5
