# EOLkits — Handoff 2026-06-23 (resume point)

_Supersedes the operational picture in earlier handoffs. Read this first to resume.
**No secrets in this file** (the repo is public) — credential **locations** only._

---

## TL;DR — where we are

- **EOLkits** (repo "Rupture", product **https://eolkits.com**) is a productized AWS end-of-life
  migration service: free MIT CLIs + a free browser scanner → **$299 Audit** → **$1,499 Migration
  Pack** → **$19/mo Drift Watch**.
- This session: full autopsy (it had earned **$0** because it was never launched **and** its paid
  fulfillment email was silently broken) → rebuilt the funnel for conversion (faceless, proof +
  guarantee) → **deployed it live** → **fixed the broken email so audits actually deliver** → stood
  up autonomous distribution + self-running infra → hit the honest limit that a paying buyer in
  **≤3 weeks** needs a human action on an account-gated channel.
- **All work is on branch `marketing-machine-v2`, pushed to `origin`. Nothing merged to `main`.**
  The static site auto-deploys daily from that branch.
- **WE PAUSED ON ONE DECISION:** will the owner spend **~15 min on Upwork** to chase live
  AWS-migration jobs? **Yes** → fastest $0 path to a 3-week buyer (I write every proposal; owner
  submits). **No** → the organic engine, **~1–2 month** first-buyer timeline. Owner has not answered.

---

## The single most important thing that changed

**Paid fulfillment was broken; it's now fixed.** `apps/grace-api` sends audit PDFs + lead alerts via
Resend `from: noreply@eolkits.com`, but **eolkits.com was never a verified Resend domain** → every
send was rejected → a paying customer would have received **nothing**. Fixed with the owner's Resend
key (on the box) + Porkbun DNS: added eolkits.com to Resend (**domain id
`e7f9a45b-5860-4169-8b98-fbef7186d93b`**), created DKIM + `send` SPF/MX records, verified, and a live
test email delivered to `hello@toledotechnologies.com`. **Audits now deliver.** This was a
precondition for *any* revenue.

---

## Live system state

| Thing | Status |
|---|---|
| **eolkits.com** | LIVE on GRACE VPS `ubuntu@15.204.209.97` (vps-6e575790); host Caddy serves `/var/www/eolkits` |
| **Static deploy** | **Self-running.** Box cron `/home/ubuntu/bin/deploy-eolkits-web`, daily `17 7 * * *` UTC: pulls `marketing-machine-v2` → builds → `sudo rsync` → IndexNow ping → auto-publishes new dev.to articles. Build workspace: `/home/ubuntu/sites/eolkits-build` (dedicated clone) |
| **Conversion funnel** | Live: home/audit/`/audit/sample`/pack/scan/migrate(×9)/drift/success. Faceless: proof-first + iron-clad guarantee (see AUTOPSY doc for locked decisions) |
| **Email fulfillment** | ✅ FIXED (Resend verified, test delivered) |
| **Content engine** | ~54 indexed URLs: 23 `/fix` error pages + 3 head-term pages (lambda-runtime-deprecation-schedule, amazon-linux-2-eol-checklist, amazon-linux-2-vs-amazon-linux-2023) + migrate/vs/blog. IndexNow-submitted. First-party `track.js` pageview beacon on content pages |
| **dev.to** | 2 articles live (canonical → eolkits.com); 3rd pending new-account daily cap (cron lands it); future articles auto-publish |
| **Weekly growth** | Cloud routine **`trig_01VpM4m1QjLPnBLZ3SaeuNKX`**, Sundays 13:00 UTC: writes+pushes 1 verified dev.to article + reports funnel/index signal. Manage: https://claude.ai/code/routines/trig_01VpM4m1QjLPnBLZ3SaeuNKX |
| **Hashnode** | Parked — GraphQL API went **paid** 2026-05-13 |
| **Payments** | Stripe live (unchanged). **A real $299 test purchase has still never been run** — the last un-derisked link (Stripe→PDF→email; email half now confirmed) |

---

## Credentials / access — LOCATIONS ONLY (no secret values; repo is public)

- **VPS:** `ssh ubuntu@15.204.209.97`. SSH key `~/.grace-keys/graceai-love.pem` (local Mac only — NOT
  available to cloud agents). Passwordless `sudo`.
- **dev.to key:** box `/home/ubuntu/.eolkits-dist.env` → `DEVTO_API_KEY` (mode 600). Read by the deploy cron.
- **Resend key:** box `/home/ubuntu/sites/eolkits-api/.env.production` → `RESEND_API_KEY`. **Free tier =
  1 domain** (eolkits.com); cannot add a `mail.` sending subdomain without upgrading.
- **Porkbun `pk1_`/`sk1_`:** owner-provided in chat, used in-session for the email-DNS fix, **NOT
  persisted** (not on box, not in repo). Re-ask owner if DNS work is needed. Endpoint
  `https://api.porkbun.com/api/json/v3` + a browser `User-Agent` (WAF). Owns 8 domains; oldest ~5.5mo
  (no aged/authority domain to 301).
- **Hashnode token:** owner has it; parked (paid API).
- **IndexNow key:** a public key file emitted by `build.py` (served at `/<key>.txt`) — not a secret.

---

## Key files (in repo, branch marketing-machine-v2)

- `AUTOPSY-AND-14-DAY-REVENUE-PLAN.md` — diagnosis, strategy, **locked faceless-conversion decisions**.
- `PROFIT-PROJECTIONS.md` — honest model (first buyer base **~late Jul–early Sep 2026**; reliable at
  the **Feb-2027 Lambda block wave**; conservative ~$0 without distribution).
- `launch/distribution/README.md` — autonomous distribution kit (dev.to publisher + 3 articles, X
  content, value-first email kit, video scripts) + the unlock list.
- `launch/distribution/fast-cash/README.md` — **the ≤3-week buyer plan** (ready-to-paste Upwork
  proposal, AWS re:Post answer, Fiverr gig, LinkedIn post). All need owner accounts to fire.
- `launch/distribution/devto/publish_devto.py` — idempotent, throttled dev.to publisher (reads `DEVTO_API_KEY`).
- `deploy/grace/ship-web.sh` — manual one-shot deploy (`--apply` = sudo rsync). `deploy/grace/cron-deploy-eolkits-web.sh` — the daily self-deploy + dev.to auto-publish (installed at `/home/ubuntu/bin/deploy-eolkits-web`).
- `apps/web/build.py` — deterministic SSG (all page builders; `/fix` corpus from `apps/web/content/fixes.yml`; head-term pages; `track.js`; IndexNow key). Build: `python3 apps/web/build.py`. Determinism + leak gates enforced; web tests `python3 -m pytest apps/web/ -q` (4) + grace-api (35).
- `apps/grace-api/eolkits_grace/email.py` — sends from `noreply@eolkits.com` via Resend. `/api/v1/lead` capture in `app.py`.

## Architecture quick-ref

- **Static site:** `apps/web/build.py` → `docs/` → `/var/www/eolkits` (GRACE host Caddy). Self-deploys
  daily from `marketing-machine-v2` via a **separate workspace clone** — do NOT `git pull` the GRACE
  backend tree on the box.
- **Paid API:** `apps/grace-api` (compose `eolkits-api`) on the box, reverse-proxied under
  `/api/* /webhook/* /upload/* /pack/* /support/* /partners/*`. State = SQLite + filesystem. Email = Resend (verified).
- **Distribution:** organic + AI-search **only** (Reddit dead per owner; HN/social/cold-email
  gated/brand-risky). dev.to backlinks + IndexNow + the corpus. The empire's bulk cold-outreach was
  removed in GRACE's rebuild (compliance) — **do not revive** (would wreck the email deliverability
  just fixed).

---

## What's left / how to resume

1. **DECISION PENDING (resume here): Upwork yes/no.**
   - **YES** → re-run the demand search (`gh search code` for `nodejs18.x`/`python3.9`; WebSearch
     Upwork AWS-migration jobs), write a tailored proposal per live job using the template in
     `fast-cash/README.md`; owner submits. Highest-probability $0 path to a buyer in ≤3 weeks.
   - **NO** → keep the engine compounding; first buyer ~1–2 months. **Leading indicator:** watch
     `eolkits.com/status` for the first `checkout_click` — that means a buyer is imminent (and if
     clicks appear with no buys, it's a conversion/trust problem to fix, not traffic).
2. **Un-derisked:** one real **$299 test purchase** (owner's card, refundable) to confirm the full
   Stripe → PDF generation → email chain end-to-end.
3. **Autonomous & ongoing (no action needed):** weekly cloud routine adds content; daily box cron
   deploys + publishes. **Quality-first — skip, don't pad** (thin/duplicate content hurts SEO).
4. **Optional, owner-gated (drafts ready in `fast-cash/`):** AWS re:Post / Stack Overflow answers,
   Fiverr gig, one LinkedIn post.

## Honest bottom line

Built, delivering (email fixed), self-running, pointed at real demand. Revenue is gated on traffic
(compounding over weeks–months) plus, ideally, **one human distribution action the owner has not yet
committed to**. No fantasy: first money realistically **~1–2 months** on the current hands-off path;
faster **only** if the owner opens one account (Upwork) or a small budget appears. The **Feb-2027
Lambda wave** is the big inflection.

## AI-resume memory
`~/.claude/projects/-Users-nicholastoledo-Development-active-Rupture/memory/`:
`eolkits-why-zero-revenue`, `eolkits-conversion-build-state`, `eolkits-deploy-and-access-constraints`,
`decision-delegation-on-strategy`.
