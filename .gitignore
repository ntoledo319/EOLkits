# Build artifacts
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.tox/
.coverage
htmlcov/
*.egg-info/
dist/
build/

# Node
node_modules/
*.tsbuildinfo
.wrangler/
apps/vscode-extension/out/
apps/vscode-extension/*.vsix

# Environment / secrets
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# ledger/internal/ is tracked but not linked from public pages.

# Local feed signing scratch
/tmp/rupture_pack.*
.aider*

# Revenue-loop scratch (WORKSPACE_ROOT/tmp per AGENTS.md §1); binaries + throwaway scripts
/tmp/
.kilo/

# Mutation testing scratch (mutmut writes mutants/, never commit)
mutants/
.mutmut-cache
mutation_report.txt

# Claude scheduled-task lock (local, host-specific)
.claude/

# Toledo tooling metadata (host-specific local path; not part of the project)
.toledo.yaml

# Type-check & lint caches
.mypy_cache/
.ruff_cache/

apps/runner/.venv/
